import styled from 'styled-components';

const NotFound = ({}) => {

  return(
    <NotFoundWrap>
      Sorry! Not found!
    </NotFoundWrap>
  );
}

const NotFoundWrap = styled.div`
  width: 100%;
  height: 100%;
`




export default NotFound;